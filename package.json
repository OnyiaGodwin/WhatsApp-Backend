{
  "name": "whatsapp-backend",
  "version": "1.0.0",
  "description": "WhatsApp Clone",
  "main": "server.js",
  "type": "module",
  "//": [
    "This is a comment",
    "NB: In the package.json file, the 'type': 'module' is added to enable files to be imported thus: import express from 'express' and not const express = require('express')"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Godwin",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "mongoose": "^8.2.1",
    "pusher": "^5.2.0"
  }
}
